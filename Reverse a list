int* reverseArray(int arr_count, int* arr, int* result_count) {
    *result_count=arr_count; 
    int j=0;
    int* a=malloc((*result_count)*sizeof(int));
    for(int i=arr_count-1;i>=0;i--,j++)
    {
        *(a+i)=arr[j];
    }
    return a;
}
