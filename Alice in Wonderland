#include<stdio.h>
int main() {
    int n,d,sum=0;
    scanf("%d",&n);
    while(n>0)
    {
        d=n%10;
        sum=sum+d;
        n=n/10;
    }
    printf("Alice must go in path-%d to find her way to home",sum);
    return 0;
}
